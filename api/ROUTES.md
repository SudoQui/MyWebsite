# Routes

- `GET /api/health` — public health check.
- `GET /api/token` — exchanges the Azure-stored Copilot Studio Direct Line secret for a short-lived Direct Line token. CORS is limited to Mustafa's portfolio domains.
